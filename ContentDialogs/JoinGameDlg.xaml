<ContentDialog x:Class="Catan10.JoinGameDlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catan10"
               xmlns:proxy="using:Catan.Proxy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Catan Join Game" PrimaryButtonText="Ok" SecondaryButtonText="Cancel"
               Width="620" Height="210"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick" mc:Ignorable="d">
    
    <Grid HorizontalAlignment="Stretch" Background="{x:Bind Player.BackgroundBrush, TargetNullValue=Blue, FallbackValue=Red}" >
        <ComboBox ItemsSource="{x:Bind GameNames}" Header="Game Names" HorizontalAlignment="Stretch" SelectedItem="{x:Bind GameSelected, Mode=TwoWay}" Foreground="{x:Bind Player.ForegroundBrush}">
            <ComboBox.ItemTemplate>
            <DataTemplate x:DataType="proxy:GameInfo">
                <TextBlock Text="{x:Bind Name}" FontSize="20"/>
            </DataTemplate>
                </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
        
</ContentDialog>
