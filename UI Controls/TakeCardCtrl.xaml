<UserControl x:Class="Catan10.TakeCardCtrl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catan10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="540" d:DesignWidth="960" mc:Ignorable="d">
    <Grid Width="960" Height="540" Background="{StaticResource DevCardType.Back}" CornerRadius="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
            <RowDefinition Height="220" />
            <RowDefinition Height="30" />
            <RowDefinition Height="220" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Viewbox Grid.Row="1">
            <TextBlock Text="Source" Foreground="White" />
        </Viewbox>
        <GridView Grid.Row="2">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:ResourceCardModel">
                    <local:ResourceCardCtrl ResourceCardModel="{x:Bind Mode=OneWay}" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Viewbox Grid.Row="3">
            <TextBlock Text="Desination" Foreground="White" />
        </Viewbox>
    </Grid>
</UserControl>
